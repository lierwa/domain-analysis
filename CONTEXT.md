# 数据抓取与清洗领域词汇表

状态：2026-08-24 当前只冻结阶段 1 术语

## 1. 抓取任务对话

### 标准商品（Standard Product）

能够通过品牌、型号、规格、分类、标准或其他稳定身份进行枚举和比较的商品，例如冰箱和电视机。手工制品、孤品和定制品等缺少稳定商品身份的非标准商品不属于当前范围。

### 抓取任务对话（Capture Task Interview）

用户与专业抓取任务顾问的对话。负责人可以用任意自然语言给出目标、回答、纠正、补充事实或追问；系统先结合当前问题和既有记录理解整条输入，再调查标准、品牌、型号、参数、部件、原理和来源等可查事实。

### 采访工作资料（Interview Working Record）

对话中持续累积的消息与有序调查活动、负责人原文、已解释或撤回的决定、未决项、搜索事实和历史 Markdown 草稿版本。它们共同作为继续采访和综合下一版草稿的依据，历史只追加或被新结论明确取代；采访资料可以是文字，不提前套正式任务 schema，也不形成第二套任务事实源。

### 采访决定（Interview Decision）

必须由负责人确认、会实质改变抓取结果的取舍。Agent 每次只提出一个问题，给出专业推荐、依据和主要代价；负责人整条原文先被理解，明确回答才形成 confirmed，成立的前提否定则撤回问题。建议只是理解上下文，不是限制输入的表单选项；追问或有歧义的文本不能被误记成选择。

### 抓取任务草稿（Capture Task Draft）

根据采访工作资料综合形成、供负责人直接阅读和修改的版本化 Markdown 范围说明。它用自然语言说明原始需求、标准商品边界、市场/时间口径、准备抓取的内容、关键调查依据、排除项和仍需系统核实的事项；品牌与官网执行入口由后续 Planning Agent 深搜。它不包含正式 Capture Task JSON 或 Crawl Plan。收到后续输入时，旧版本保留为历史但不再是当前确认对象；只有最新完整版本可被显式确认，它不能直接执行。

### 抓取任务（Capture Task）

用户整体确认 Markdown 范围草案后，由 Workbench 单独结构化生成的抓取范围事实源。结构化步骤只能忠实转换已确认文字，不得继续搜索、提问或补充事实。首次确认创建任务；以后继续原对话并再次确认时，任务 ID 不变、当前 revision 前进，历史确认草案保持不可变。确认任务不等于制定计划或开始抓取。

## 2. 计划与来源

### 平台来源策略（Marketplace Source Policy）

某类标准商品是否存在可安全执行的平台来源的调查结论，不是为了凑问答而制造的 Interview Decision。当前京东搜索、登录与目录覆盖均无法提供可信分母，新 version 4 计划不执行京东；平台以后只有在存在公开、可审计、经确认的来源时才进入计划。品牌、官网、参数、说明书、标准和原理来源由 Planning Agent 主动深度调查。此前真实失败运行已否决把全部调查和来源重写塞进单次巨型最终 JSON；当前按 R-045 先做六镜头品牌发现和逐查询饱和核查，再用可配置小批独立上下文核对官网/参数，最后由 Workbench 确定性组装 v4 Plan。历史电视 v9 及其四批 Source Dataset 只保留为失败和内容审计证据，不是当前执行基线。

### 抓取计划（Crawl Plan）

面向一个 Capture Task revision 的版本化、可执行来源计划。它通过具体来源与抓取目标确定入口、Provider key/version 与已校验配置、捕获内容、数量、覆盖分母、访问限制、原始输出和停止口径；确认计划不等于开始抓取。

### 抓取规划运行（Crawl Planning Run）

把一个已确认 Capture Task revision 转成 Crawl Plan Draft 的一次可见 Agent 运行。它先冻结品牌分母，再按 1-10 个品牌的小批独立上下文调查官网与参数/说明书，最后调查标准监管和技术原理；全部小 schema 通过后才确定性组装并保存 Research Audit。DBOS 用稳定阶段 ID 让完成阶段跨页面和进程恢复，Workbench Stage Checkpoint 保存用户可见进度；它记录调查过程和状态，不拥有任务范围，也不是正式来源运行。

### 规划研究账（Planning Research Audit）

version 4 Crawl Plan 中对 AI 深度调查的可审核记录；其研究策略仍为 v3：市场口径、四类搜索 pass、规范品牌清单、逐品牌官网映射或 unresolved、task topic 到 source key 的覆盖关系、完整性状态和停止理由。它是计划事实，不是已抓取原始事实；搜索 URL 仍只表示 `search_discovered/unknown`。

### 抓取目标（Capture Target）

Crawl Plan 中“在一个具体来源抓什么、抓多少”的最小单元。它绑定任务内容方向、捕获单元、原始格式、数量口径、唯一键、路由、内容验收和停止条件。`exact` 只读取计划 URL；`site` 只在计划冻结的同源、深度、页数、时长和请求预算内发现页面。

### 候选来源（Source Candidate）

在已确认 Markdown 草案结构化为正式 Capture Task 时形成、可能用于后续规划的来源入口。它只能来自采访记录中已有的搜索事实，记录发布者、有效 URL、来源类型、预期内容、当前已知访问状态和记录时间；采访中的搜索发现可以先保留为文字，不能冒充页面已打开、已授权、已接入或已抓取。

### 来源观察时间（Source Observed At）

Workbench 把采访中已有的来源事实结构化为正式 Source Candidate 的时点。它不是模型自行生成的网页访问时间，也不能替代后续 Raw Source Observation 的真实访问时间。

### Provider

负责一个外部来源的访问能力。Provider 执行冻结计划、识别页面/接口状态并返回源站原始内容；当前公共 Provider 复用 Crawlee sitemap parser 与持久 RequestQueue 执行 `exact/site` 两条显式路由。它不决定抓取目标，不清洗数据，也不定义跨品类参数。

### 来源对象（Source Object）

外部来源中可以稳定识别的业务对象，例如分类页、商品详情、说明书、监管记录或评论页。

### 来源运行（Source Run）

一个来源在一次正式抓取批次中的执行。它绑定抓取任务、执行批次、计划来源项、Provider 和访问策略，记录开始、结束、状态和计数。历史运行可能没有批次关系，必须明确标成“历史记录（无批次）”，不能按相近时间猜测归属。

### 抓取批次（Source Collection Batch）

负责人每次点击“开始新批次抓取”后、任何 Provider preflight 或来源访问前创建的一次可审计执行事实。它冻结任务 revision、计划 ID/version、计划来源数、开始/结束时间和批次结果，并拥有本轮全部 Source Run；Prepare 只检查条件，不创建批次。

### 来源运行恢复（Source Run Resume）

负责人对 stopped/failed Source Run 发起的显式继续。系统创建新的 Source Run，并用 `resumedFromRunId` 关联前序运行；前序已完成工作不重复，请求预算、窗口和冷却不能因继续而重置。它不是自动重试。

### 捕获工作项（Capture Work Item）

Provider 从已确认入口或前序原始响应中发现的、确实需要网络访问的动态捕获对象。它用稳定 work key 对账 pending/running/completed/failed/stopped 和预期/实际数量；图片 URL 引用不是工作项。

### 来源请求尝试（Source Request Attempt）

一个实际 HTTP hop 的持久账本记录。它在网络发送前先占预算并记录 requested URL、Provider、run/work/target 和开始时间，随后补充响应或停止结果；没有 attempt 就不得出网。

### 来源访问门（Source Access Gate）

PostgreSQL 中跨进程共享的请求预算、最小间隔、窗口、冷却、熔断和人工继续状态。进程内 limiter 只能执行其准入结果，不能另算一套。

## 3. 原始数据

### 来源观察（Raw Source Observation）

一次访问的事实记录：请求 URL、最终 URL、观察时间、HTTP 状态、响应头摘要、访问状态和错误。访问成功不表示数据有用或完整。

### 原始来源载荷（Raw Source Payload）

源站在当时返回的业务内容。可以是 HTML、JSON、CSV、文本，或 PDF、XLSX、图片、视频等文件；格式由来源决定，不强制套统一参数模板。

### 来源快照（Source Snapshot）

来源对象在某次运行中的不可变观察和原始载荷。新抓取追加新快照，不覆盖历史。

### 来源资源引用（Source Resource Reference）

来源快照中观察到、但当前阶段没有下载的外部资源引用。它记录原始 URL、资源类型、在原内容中的用途、区块、顺序及所属来源对象关系；它不是来源附件，不表示资源字节已经取得、URL 仍可访问或内容已经校验。

### 来源附件（Source Asset）

以原文件保存的 PDF、表格、图片、视频或其他二进制内容，带来源 URL、媒体类型、字节数和内容哈希。

### 来源数据集（Source Dataset）

一个抓取任务下所有来源运行、对象、快照、资源引用和附件的集合。Workbench 可以查看并导出；它不是清洗结果。

### 规划研究审计（Planning Research Audit）

version 4 Crawl Plan 随附的结构化规划证据：记录品牌版图、官网映射、参数/说明书、标准/原理四类搜索的查询、证据 URL、发现与停止口径，并逐品牌和逐 task topic 对账。它属于计划版本，不是 Source Dataset，也不能证明页面已经正式抓取。

### 未解决品牌（Unresolved Brand）

Planning Agent 已发现但无法核实官网公开入口的品牌。它必须保留品牌名、发现证据和原因，使计划保持 `partial`；不得用猜测站点或搜索结果页伪造可执行来源。修复需要新的 Planning Run 和计划版本，不能由 Source Run 自动扩展范围。

### 旧结构化记录（Legacy Structured JSON）

清理前已经保存的结构化来源内容。为了不删除本地历史数据而保留，并明确标记为 legacy；它不能作为新原始捕获格式的示范。

## 4. 停止与边界

### 访问停止条件

登录要求、验证码、风控、拒绝访问、异常响应、许可不明或频控熔断。系统必须停止并记录，不得自动绕过。

### 阶段 2 数据清洗

未来对不可变原始数据做解析、去噪、标准化、合并和质量判断的阶段。当前尚未设计或实现。

## 5. 禁止混用

- 抓取任务草稿 ≠ 抓取计划；前者说抓什么和从哪里，后者说具体怎样执行。
- 抓取任务草稿 ≠ Capture Task JSON；前者是可读 Markdown，后者只在确认后单独结构化。
- 抓取规划运行 ≠ 来源运行；前者生成计划，后者按已确认计划取得原始数据。
- 抓取任务确认 ≠ 开始抓取。
- 抓取计划确认 ≠ 开始抓取。
- 抓取条件检查 ≠ 创建抓取批次或访问来源。
- 一个抓取任务 ≠ 只能有一个计划或一个批次；市场发现、官网补齐和缺口修复可以使用多个已确认计划版本，但共同投影为一个任务的覆盖状态。
- 抓取批次 ≠ 来源运行；前者对应一次 Start，后者对应该批次中的一个来源。
- 来源可访问 ≠ 数据完整、可用或允许再分发。
- Source Dataset ≠ 清洗结果。
- Planning Research Audit ≠ 已正式抓取的来源数据，也不是阶段 2 知识库。
- JSON/Zod schema ≠ 用户配置面板；它只负责内部边界校验。
