# 数据抓取与清洗领域词汇表

状态：2026-08-18 当前只冻结阶段 1 术语

## 1. 抓取任务对话

### 抓取任务对话（Capture Task Interview）

用户与专业数据抓取 Agent 的对话。用户给目标和取舍，系统负责调查品牌、型号、参数、部件、原理和来源等可查事实。

### 采访决定（Interview Decision）

必须由负责人确认的取舍。Agent 每次只提出一个问题，提供 2–3 个互斥选项和唯一推荐项；只有界面显式确认才成为 confirmed。

### 抓取任务草稿（Capture Task Draft）

对话产出的版本化方案，说明原始需求、商品门类、市场口径、准备抓取的内容、京东意向、候选来源、排除项和未决项。继续对话会生成新版本，旧版本不覆盖。它不是参数 schema，也不能直接执行。

### 抓取任务（Capture Task）

用户整体确认后的抓取范围事实源。首次确认创建任务；以后继续原对话并再次确认时，任务 ID 不变、当前 revision 前进，历史确认版本保持不可变。确认任务不等于开始抓取。

## 2. 计划与来源

### 抓取计划（Crawl Plan）

面向一个已确认抓取任务和具体来源的可执行计划。它声明捕获单元、对象范围、覆盖分母、数量、唯一键、分页/遍历终止、频控、恢复、持久化和导出。

### 候选来源（Source Candidate）

在任务对话阶段真实调查过、可能用于后续抓取的入口。记录发布者、URL、来源类型、预期内容、观察格式、访问状态和观察时间。候选不等于已授权或已接入。

### Provider

负责一个外部来源的访问能力。Provider 执行冻结计划、识别页面/接口状态并返回源站内容；它不决定抓取目标，不清洗数据，也不定义跨品类参数。

### 来源对象（Source Object）

外部来源中可以稳定识别的业务对象，例如分类页、商品详情、说明书、监管记录或评论页。

### 来源运行（Source Run）

一次正式抓取执行。它绑定抓取任务、计划来源项、Provider 和访问策略，记录开始、结束、状态和计数。

## 3. 原始数据

### 来源观察（Raw Source Observation）

一次访问的事实记录：请求 URL、最终 URL、观察时间、HTTP 状态、响应头摘要、访问状态和错误。访问成功不表示数据有用或完整。

### 原始来源载荷（Raw Source Payload）

源站在当时返回的业务内容。可以是 HTML、JSON、CSV、文本，或 PDF、XLSX、图片、视频等文件；格式由来源决定，不强制套统一参数模板。

### 来源快照（Source Snapshot）

来源对象在某次运行中的不可变观察和原始载荷。新抓取追加新快照，不覆盖历史。

### 来源附件（Source Asset）

以原文件保存的 PDF、表格、图片、视频或其他二进制内容，带来源 URL、媒体类型、字节数和内容哈希。

### 来源数据集（Source Dataset）

一个抓取任务下所有来源运行、对象、快照和附件的集合。Workbench 可以查看并导出；它不是 Evidence，也不是清洗结果。

### 旧结构化记录（Legacy Structured JSON）

清理前已经保存的结构化来源内容。为了不删除本地历史数据而保留，并明确标记为 legacy；它不能作为新原始捕获格式的示范。

## 4. 停止与边界

### 访问停止条件

登录要求、验证码、风控、拒绝访问、异常响应、许可不明或频控熔断。系统必须停止并记录，不得自动绕过。

### 阶段 2 数据清洗

未来对不可变原始数据做解析、去噪、标准化、合并和质量判断的阶段。当前尚未设计或实现。

## 5. 禁止混用

- 抓取任务草稿 ≠ 抓取计划；前者说抓什么和从哪里，后者说具体怎样执行。
- 抓取任务确认 ≠ 开始抓取。
- 来源可访问 ≠ 数据完整、可用或允许再分发。
- Source Dataset ≠ Evidence ≠ 清洗结果。
- JSON/Zod schema ≠ 用户配置面板；它只负责内部边界校验。
