{
  "name": "@domain-analysis/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "ensure:local": "node --env-file=../../.env.example --import=tsx scripts/ensure-local-database.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "generate:workbench": "drizzle-kit generate --config drizzle.workbench.config.ts"
  },
  "dependencies": {
    "@domain-analysis/shared": "0.1.0",
    "drizzle-orm": "0.45.2",
    "pg": "8.23.0"
  },
  "devDependencies": {
    "@types/pg": "8.21.0"
  }
}
